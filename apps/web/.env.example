# Database
# Used by @benkyou/db for Drizzle queries and migrations.
DATABASE_URL=

# Auth
# Generate with: npx -y @better-auth/cli secret
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000

# AI generation
# Optional until the real generation worker replaces the sample/mock path.
AI_PROVIDER=
AI_API_KEY=
